query LayoutWithSpecs($appName: String!) {
  layoutWithSpecs (
    appName:$appName
  ) {
    cacheId
    layoutWithSpecs {
      spec
      specLocator {
        appId
        specName
      }
    }
  }
}
