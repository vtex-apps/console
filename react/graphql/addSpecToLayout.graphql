mutation AddSpecToLayout($appName: String!, $specLocatorInput: SpecLocatorInput!) {
  addSpecToLayout(appName: $appName, specLocators: ) {
    cacheId
    layout {
      spec
      specLocator {
        appId
        specName
      }
    }
  }
}
